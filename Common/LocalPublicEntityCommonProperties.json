{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$comment": "This JSON file provides regex patterns for local public entities.",
    "definitions": {
        "StandardTransliteration": {
            "type": "string",
            "pattern": "^[a-zA-Z\\s]*$",
            "description": "English name of the local public entity."
        },
        "PhoneticTransliteration": {
            "type": "string",
            "pattern": "^[a-zA-ZÔô\\s]*$",
            "description": "English name of the local public entity."
        },
        "ZenkakuRegexPattern": {
            "type": "string",
            "pattern": "^[ァ-ヿ]*$",
            "description": "全角カナ、拗音・促音は小書き"
        },
        "HankakuRegexPattern":{
            "type": "string",
            "pattern": "^[ｦ-ﾟ]*$",
            "description": "半角カナ、拗音・促音は大書き"
        }
    }
}